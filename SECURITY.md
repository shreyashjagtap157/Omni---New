# Security

Security-sensitive implementation changes require explicit provenance, verification evidence, and regression coverage. Unsafe Rust is forbidden at the workspace level unless an explicitly approved implementation exception is established by the project policy.
