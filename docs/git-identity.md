# Git Identity

The initial `OMNI-IMP-0.0.0.1` repository commit uses the repository-local technical identity `Omni Bootstrap <omni-bootstrap@localhost>` because the execution host had no configured Git author identity.

This does not assert human authorship. A project maintainer may amend or rewrite the commit identity before publication without changing the implementation contents.
