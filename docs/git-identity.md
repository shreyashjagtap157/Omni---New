# Git Identity

The authoritative GitHub account for this project is:

- GitHub login/username: `shreyashjagtap157`
- GitHub profile display name: `warrite`
- Commit email: `ssjagtap2016@gmail.com`

The repository-local Git configuration must use:

```text
user.name = shreyashjagtap157
user.email = ssjagtap2016@gmail.com
user.useConfigOnly = true
```

The GitHub login `shreyashjagtap157` is the appropriate project identity for repository links and repository ownership. `warrite` is the profile display name and should not be substituted for the login when constructing GitHub repository URLs.

The initial `OMNI-IMP-0.0.0.1` commit was created before this identity was available on the execution host and therefore retains the technical bootstrap identity `Omni Bootstrap <omni-bootstrap@localhost>`. Existing history is intentionally not rewritten automatically. All subsequent project commits use the maintainer identity above.
